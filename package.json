{
  "name": "workflow-auto",
  "version": "1.0.0",
  "description": "Workflow Builder Lite - automation runner",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "dependencies": {
    "gitignore": "^0.7.0"
  }
}
